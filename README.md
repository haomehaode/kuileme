# 亏了么 (Kuleme)

一个记录投资亏损、分享心路历程的社交应用。

## 项目结构

```
kuileme/
├── app/              # Flutter 移动应用
├── server/           # Python FastAPI 后端服务
└── 需求文档.md       # 项目需求文档
```

## 功能特性

### 用户认证
- ✅ 手机号验证码登录
- ✅ 账号密码登录
- ✅ 用户注册
- ✅ 密码重置

### 核心功能
- ✅ 亏友圈发帖（支持图片、标签、心情）
- ✅ 首页数据展示（亏损统计、回血抽奖）
- ✅ 账单查看（时间筛选、分布图表、分享）
- ✅ 个人中心（成长数据、勋章墙、我的日记）
- ✅ 存钱抽奖回血
- ✅ 紧急救助站
- ✅ 投资分析复盘

### 社交功能
- ✅ 帖子点赞、评论、心碎
- ✅ 通知系统
- ✅ 账单分享

## 快速开始

### 移动应用 (Flutter)

```bash
cd app
flutter pub get
flutter run
```

#### iOS 设备连接

**模拟器**：
```bash
flutter run -d "iPhone 15 Pro"
```

**真机**：
1. 确保设备和电脑在同一网络
2. 修改 `app/lib/services/api_service.dart` 中的 `baseUrl` 为你的电脑 IP
3. 运行：`flutter run -d "你的设备名称"`

### 后端服务 (Python)

#### 方式一：使用启动脚本（推荐）

```bash
cd server
chmod +x start.sh
./start.sh
```

#### 方式二：手动启动

```bash
cd server
# 激活虚拟环境（如果使用）
source activate.sh  # 或 python -m venv venv && source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

服务启动后访问：
- API 文档：http://localhost:8000/docs
- API 地址：http://localhost:8000

## 构建说明

### iOS 构建

```bash
cd app
flutter build ipa --release
```

构建完成后，IPA 文件位于：`app/build/ios/ipa/kuleme.ipa`

### Android 构建

```bash
cd app
flutter build apk --release
```

构建完成后，APK 文件位于：`app/build/app/outputs/flutter-apk/app-release.apk`

## 技术栈

### 前端
- **框架**: Flutter 3.7.0+
- **主要依赖**:
  - `http`: HTTP 请求
  - `shared_preferences`: 本地存储
  - `share_plus`: 分享功能

### 后端
- **框架**: Python FastAPI
- **主要依赖**:
  - `fastapi`: Web 框架
  - `sqlalchemy`: ORM
  - `pydantic`: 数据验证
  - `python-jose`: JWT 认证
  - `passlib[bcrypt]`: 密码加密
  - `uvicorn`: ASGI 服务器

### 数据库
- SQLite（开发环境，默认）
- PostgreSQL（生产环境，可选）

## 开发

### 环境要求

- Flutter 3.7.0+
- Python 3.11+
- Xcode (iOS 开发)
- Android Studio (Android 开发)

### 开发配置

#### iOS 开发
- 已配置 App Transport Security (ATS) 允许本地 HTTP 连接
- 配置文件：`app/ios/Runner/Info.plist`

#### Android 开发
- 网络权限已配置
- 支持 HTTP 连接（开发环境）

### API 配置

默认 API 地址：`http://localhost:8000`

如需修改，可在 `app/lib/services/api_service.dart` 中调整 `ApiConfig.baseUrl`

### 默认测试账号

- 手机号：`123456`
- 密码：`123456`
- 验证码（开发环境）：`123456`

## 项目特色

- 🎨 现代化 UI 设计（深色主题、玻璃态效果）
- 📊 数据可视化（圆环图、统计图表）
- 🔐 安全的用户认证（JWT + bcrypt）
- 📱 响应式布局
- 🎯 完整的社交功能

## License

MIT
